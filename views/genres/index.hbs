<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{page-title}} - Gestor de Cine</title>
   
</head>
<!-- Hero Section -->

    <div class="container">
        <div class="row">
            <div class="col text-center">
                <h1 class="display-4 fw-bold">{{page-title}}</h1>
                <p class="lead text-muted">Explora y administra tu colección de géneros favoritos.</p>
            </div>
        </div>
    </div>

<!-- Action Bar -->
<div class="row mb-4">
    <div class="col-md-6">
        <h3 class="mb-0">Listado de Géneros</h3>
        <p class="text-muted">Gestiona todos los géneros disponibles</p>
    </div>
    <div class="col-md-6 text-end">
        <a href="/generos/nuevo" class="btn btn-primary btn-lg">
        <i class="bi bi-journal-plus"></i> Agregar Género
        </a>
        </div>
    </div>



<div class="row g-4">
    {{#each genres}}
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
        <div class="card h-100 shadow-sm hover-shadow">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">{{this.nombre}}</h5>
                <p class="card-text text-muted small flex-grow-1">
                    {{#if this.descripcion}}
                    {{this.descripcion}}
                    {{else}}
                    Género de cine
                    {{/if}}
                </p>
                <div class="d-flex gap-2 mt-auto">
                    <a href="/generos/edit/{{this.id}}" class="btn btn-sm btn-outline-primary flex-fill">
                    <i class="bi bi-pencil-fill"></i> Editar
                    </a>
                    <form method="post" action="/generos/delete" class="flex-fill">
                        <input type="hidden" value="{{this.id}}" name="GenreId" />
                        <button type="submit" class="btn btn-sm btn-outline-danger w-100 delete-genre-btn">
                        <i class="bi bi-trash"></i>   Borrar  </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    {{else}}
    <div class="col-12">
        <div class="alert alert-info text-center" role="alert">
            <h4 class="alert-heading">No hay géneros disponibles</h4>
            <p>Comienza agregando tu primer género de cine.</p>
            <hr>
            <a href="/generos/nuevo" class="btn btn-primary">Agregar Género</a>
        </div>
    </div>
    {{/each}}
</div>





